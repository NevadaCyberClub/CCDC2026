---
- name: Gather facts to load OS information
  tags: always
  setup:
  become: no

- name: Check system status
  include_tasks: sysinfo.yml

- name: launch nmap scan
  include_tasks: nmap.yml

- name: bunkerbweb
  include_tasks: bunkerbweb.yml